import { makeStyles } from "@material-ui/styles";
const useStyles = makeStyles({
  root: {
    paddingTop: "2rem",
  },
  titleInput: {
    marginBottom: "2rem",
    width: "100%",
  },
  titleLabel: {
    fontSize: "2rem",
  },
  buttonSave: {
    marginTop: "2rem",
    marginRight: "1rem",
  },
  message: {
    textAlign: "center",
  },
});

export default useStyles;
